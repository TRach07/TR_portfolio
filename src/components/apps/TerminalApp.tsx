/**
 * Terminal application wrapper — renders the Terminal inside an OS window.
 */

"use client";

import { Terminal } from "@/components/terminal/Terminal";

export function TerminalApp() {
  return <Terminal />;
}
